/**
 * Enumeration of the types of fields that are available during the
 * provide credentials step of connecting a Hypersync app.
 */
export enum CredentialFieldType {
  Group = 'group',
  Hidden = 'hidden',
  Password = 'password',
  Select = 'select',
  Text = 'text',
  TextArea = 'textArea'
}
